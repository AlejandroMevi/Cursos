package com.example.cursos.udemyCursos

class Notas {

    /*Broadcast Receivers
    * Se recibe informacion del dispositivo
    * como memoria, bateria, etc
    * */

    /*Intents
    * Crea la accion a realizar
    * facilita la comunicacion entre componentes
    * */

    /*Content providers
    *Compartir datos entre aplicaciones
    *  */

    /*Diferencias entre array, set, list y map
    *
    * 1. Arreglos (Arrays):
       - Los arreglos son colecciones ordenadas de elementos del mismo tipo.
       - Tienen un tamaño fijo una vez creados.
       - Puedes acceder a elementos individuales mediante su índice (empezando en 0).
       - La sintaxis para crear un arreglo es `val array: Array<Tipo> = arrayOf(elemento1, elemento2, ...)`.
       *
    2. Conjuntos (Sets):
       - Los conjuntos son colecciones desordenadas de elementos únicos.
       - No permiten duplicados, por lo que cualquier elemento aparecerá solo una vez en el conjunto.
       - No tienen un orden específico, por lo que no puedes acceder a elementos por índice.
       - Puedes crear un conjunto usando `val set: Set<Tipo> = setOf(elemento1, elemento2, ...)`.
    3. Listas (Lists):
       - Las listas son colecciones ordenadas de elementos que pueden contener duplicados.
       - Puedes acceder a elementos por índice.
       - Proporcionan operaciones de inserción y eliminación en posiciones específicas.
       - Puedes crear una lista mediante `val list: List<Tipo> = listOf(elemento1, elemento2, ...)`.
    4. Mapas:
       - Los mapas son colecciones de pares clave-valor, donde cada clave está asociada a un valor.
       - Las claves deben ser únicas, pero los valores pueden repetirse.
       - Los mapas no tienen un orden específico, pero puedes acceder a valores usando sus claves.
       - Puedes crear un mapa con `val map: Map<Clave, Valor> = mapOf(clave1 to valor1, clave2 to valor2, ...)`.
    En resumen, la elección entre arreglos, conjuntos, listas y mapas depende de tus necesidades específicas.
    * Si necesitas elementos ordenados con acceso por índice, elige arreglos o listas.
    * Si buscas colecciones únicas y no te importa el orden, opta por conjuntos.
    * Para almacenar asociaciones clave-valor, los mapas son la elección adecuada.
    * */

}